{
  "$comment": "SINGLE SOURCE OF TRUTH for the unified error model. Edit here, then run `node scripts/gen-error-codes.mjs` to regenerate the JS + Rust mirrors; CI runs it with --check to fail on drift. Codes are a CLOSED set: stable, never renamed, additive only (like invariant ids). Each code has ONE fixed domain; the Fluent key is `${domain}-err-${code}`. `title` is a STABLE, per-type, operator/developer-facing English summary (RFC 9457 title; NOT the localized UI text — that is the Fluent message keyed by code). `args` are typed and drawn ONLY from counts/limits/seconds/enums — never request/user content. `retryAfter` is a top-level AppError field, never an arg. `action` is top-level, never an arg. Coherence rule enforced by codegen: a retriable code may not carry action 'contact'/'reauth'/'upgrade'.",
  "actions": ["retry", "reauth", "bootstrap", "upgrade", "contact"],
  "domains": ["sync", "auth", "vault", "storage", "media", "app"],
  "codes": {
    "below_gc_floor":      { "domain": "sync", "title": "Below the GC floor", "retriable": false, "action": "bootstrap", "note": "write/read below the server GC floor; the client must bootstrap from a snapshot (409/410)." },
    "quota_exceeded":      { "domain": "sync", "title": "Storage quota exceeded", "retriable": false, "action": "upgrade", "args": [{ "name": "limit", "type": "u64" }, { "name": "used", "type": "u64" }], "roleGatedArgs": true, "note": "owner capacity gate; the numeric args are role-gated (F1) — only >= BILLING_ARG_MIN_ROLE (default Maintainer) receives them." },
    "rate_limited":        { "domain": "sync", "title": "Rate limited", "retriable": true, "action": "retry", "note": "429; carries top-level retryAfter (role-gated precision for non-owners, F1). Driver backs off; UI shows only if residual." },
    "version_conflict":    { "domain": "sync", "title": "Version conflict", "retriable": true, "action": "retry", "note": "409 CAS conflict; pull the current snapshot and retry." },
    "covered_anomaly":     { "domain": "sync", "title": "Coverage inconsistency", "retriable": false, "action": "contact", "note": "client-produced coverage inconsistency (over-claim / regression / below-floor) — an internal invariant break the user can't act on." },
    "head_rollback":       { "domain": "sync", "title": "Head pointer moved backward", "retriable": false, "action": null, "note": "409: a heads/{replica} pointer PUT whose count is below the stored count (anti-rollback/griefing, OPE-411). The honest owning replica never lowers its head; a monotonic advance is unaffected." },
    "invalid_request":     { "domain": "sync", "title": "Invalid request", "retriable": false, "action": "contact", "note": "400 catch-all: a malformed/rejected write (bad envelope, wrong kind, id mismatch). Static detail, never library-error text (A3)." },
    "access_denied":       { "domain": "sync", "title": "Access denied", "retriable": false, "action": null, "note": "403: member exists but under-role / removed. Deliberately distinct from not_found (F2)." },
    "not_found":           { "domain": "sync", "title": "Not found", "retriable": false, "action": null, "note": "404: the tree/object is absent. Deliberately distinct from access_denied (F2 existence distinction)." },
    "unavailable":         { "domain": "sync", "title": "Service unavailable", "retriable": true, "action": "retry", "note": "5xx / backend (R2/Neon/Lambda) down; collapsed server-side, never raw. Retriable." },
    "request_failed":      { "domain": "sync", "title": "Request failed", "retriable": true, "action": "retry", "note": "client-synth: fetch rejected (network/DNS/CORS). Honest label (C4); navigator.onLine may refine to 'offline'." },
    "offline":             { "domain": "sync", "title": "Offline", "retriable": true, "action": "retry", "note": "client-synth refinement of request_failed when navigator.onLine is false." },
    "timeout":             { "domain": "sync", "title": "Request timed out", "retriable": true, "action": "retry", "note": "client-synth: the request deadline elapsed (C2). Distinct from request_failed." },
    "auth_required":       { "domain": "auth", "title": "Authentication required", "retriable": false, "action": "reauth", "note": "401 after one refresh, or no active session. Emitted by the HTTP adapter AND the auth seam — one code, domain follows the code not the route." },
    "session_expired":     { "domain": "auth", "title": "Session expired", "retriable": false, "action": "reauth", "note": "the provider reports an expired/invalid session." },
    "sign_in_failed":      { "domain": "auth", "title": "Sign-in failed", "retriable": false, "action": null, "note": "generic sign-in failure (wrong credentials, provider refusal)." },
    "sign_up_failed":      { "domain": "auth", "title": "Sign-up failed", "retriable": false, "action": null, "note": "generic sign-up failure. 'email already registered' maps HERE by default (F3, anti-enumeration)." },
    "email_taken":         { "domain": "auth", "title": "Email already registered", "retriable": false, "action": null, "note": "RESERVED (F3): unused by default; the auth adapter's EMAIL_TAKEN_DISCLOSURE switch flips sign_up_failed → this for explicit feedback." },
    "recipient_pin_mismatch": { "domain": "auth", "title": "This invite is for a different email", "retriable": false, "action": "reauth", "note": "403 on PUT /invites/{id}/claim: the invite is pinned to a specific verified email and the caller's verified email is absent or does not match. Honest-server hardening for recipient-pinned invites (OPE-451); unpinned bearer invites are unaffected." },
    "unregistered":        { "domain": "auth", "title": "Account not registered", "retriable": false, "action": "reauth", "note": "403: the token is VALID but its subject has no account binding yet (no identities row) — the client must complete registration. Deliberately distinct from auth_required (the token itself is fine)." },
    "stale_timestamp":     { "domain": "auth", "title": "Registration timestamp out of window", "retriable": false, "action": null, "note": "401 on register: the proof-of-possession timestamp is outside the +/-5min window (clock skew) — the register flow re-signs with a fresh timestamp." },
    "bad_signature":       { "domain": "auth", "title": "Registration proof invalid", "retriable": false, "action": "contact", "note": "401 on register: the Ed25519 proof-of-possession did not verify — a wrong key / client-construction bug, not user-actionable." },
    "member_id_mismatch":  { "domain": "auth", "title": "Member id not self-certifying", "retriable": false, "action": "contact", "note": "400 on register: the supplied member_id is not uuid8(SHA-256(author_pubkey)) — a client-construction bug, not user-actionable." },
    "identity_conflict":   { "domain": "auth", "title": "Identity already bound", "retriable": false, "action": null, "note": "409 on register: the subject or member_id is already bound to a different identity — a genuine conflict the caller must surface, not retry." },
    "wrong_passphrase":    { "domain": "vault", "title": "Wrong passphrase", "retriable": false, "action": null, "note": "unlock/change/recover: the passphrase did not derive the key." },
    "tampered_anchor":     { "domain": "vault", "title": "Tree verification failed", "retriable": false, "action": "contact", "note": "the keyring anchor failed verification — out-of-date or tampered." },
    "revision_rollback":   { "domain": "vault", "title": "Version rollback detected", "retriable": false, "action": null, "note": "the anti-rollback watermark rejected an older keyring revision — a tamper/stale-restore signal, distinct from a wrong passphrase. Shared code across the web worker and the Tauri host (which already reports this code)." },
    "generation_rollback": { "domain": "vault", "title": "Account backup rolled back", "retriable": false, "action": null, "note": "a below-floor account keystore backup was refused — the server (409 on PUT /account/keystore) or the client (loading a blob below its generation floor). Credential-revocation anti-rollback; the keystore analogue of revision_rollback." },
    "recovery_code_invalid": { "domain": "vault", "title": "Invalid recovery code", "retriable": false, "action": null, "note": "the recovery code did not validate." },
    "keyring_verify_failed": { "domain": "vault", "title": "Keyring verification failed", "retriable": false, "action": "contact", "note": "a peer keyring/membership failed the verify gate." },
    "decrypt_failed":      { "domain": "vault", "title": "Decryption failed", "retriable": false, "action": "contact", "note": "an envelope failed to open/authenticate." },
    "storage_quota":       { "domain": "storage", "title": "Local storage full", "retriable": false, "action": "contact", "note": "IndexedDB/OPFS out of space." },
    "storage_blocked":     { "domain": "storage", "title": "Local storage unavailable", "retriable": false, "action": "contact", "note": "local storage unavailable/blocked (private mode, permissions)." },
    "storage_corrupt":     { "domain": "storage", "title": "Local storage corrupt", "retriable": false, "action": "contact", "note": "local store corrupt — needs a rebuild from the remote." },
    "worker_unavailable":  { "domain": "app", "title": "Background worker unavailable", "retriable": false, "action": "upgrade", "note": "the core Web Worker failed to instantiate (WebAssembly.CompileError/OOM) or went silent (C3)." },
    "internal":            { "domain": "app", "title": "Internal error", "retriable": false, "action": "contact", "note": "catch-all: an unexpected client-side failure (wasm trap, invariant break, plain-JS bug). cause is dev-log-only." }
  }
}
